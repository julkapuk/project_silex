
{% extends 'base.twig' %}

{% block title %}{{ 'Registration'|trans }}{% endblock %}

{% block content %}
    <h1>
        {{ 'Registration'|trans }}
    </h1>
    {{ error }}
    <form action="{{ path('auth_login_check') }}" method="post">
        {{ form_widget(form) }}
        <input type="submit" name="submit" value="{{ 'join'|trans }}" />
    </form>
{% endblock %}