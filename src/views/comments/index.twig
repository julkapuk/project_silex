{# Index action template for CommentsController #}
{% extends 'base.twig' %}

{% block title %}{{  'Comments list'|trans }}{% endblock %}

{% block content %}
    {#<h1>#}
        {#{{ 'Comments list'|trans }}#}
    {#</h1>#}
    {#<p>#}
        {#<a href="{{ url('comments_add') }}" title="{{ 'Add ?! album'|trans }}">#}
            {#{{ 'Add album'|trans }}#}
        {#</a>#}
    {#</p>#}

    {#{%  if albums|length > 0 %}#}

        {#<div>#}
            {#{% if paginator.page > 1 %}#}
                {#{% set previous = (paginator.page - 1) %}#}
                {#<a href="{{ url('comments_index', {'page': previous}) }}" title="{{ 'previous page'|trans }}">{{ 'previous page'|trans }}</a>#}
            {#{%  endif %}#}

            {#{% if paginator.page < paginator.pagesCount %}#}
                {#{% set next = (paginator.page + 1) %}#}
                {#<a href="{{ url('comments_index', {'page': next}) }}" title="{{ 'next page'|trans }}">{{ 'next page'|trans }}</a>#}
            {#{% endif  %}#}
        {#</div>#}

        <table>
            <thead>
            <tr>
                <th>{{ 'Id'|trans }}</th>
                <th>{{ 'Comments'|trans }}</th>
                <th>{{ 'User'|trans }}</th>
                <th colspan="3">&nbsp;</th>
            </tr>
            </thead>
            <tbody>
            {% for comment in comments %}
                <tr>
                    <td>{{ comment.id|e }}</td>
                    <td>{{ comment.comment|e }}</td>
                    <td>{{ comment.login|e }}</td>
                    {#<td>#}
                        {#<a href="{{ url('albums_view', {'id': comment.id}) }}" title="{{ 'View album'|trans }}">{{ 'View album'|trans }}</a>#}
                    {#</td>#}
                    {#<td>#}
                        {#<a href="{{ url('albums_edit', {'id': comment.id}) }}" title="{{ 'Edit album'|trans }}">{{ 'Edit album'|trans }}</a>#}
                    {#</td>#}
                    {#<td>#}
                        {#<a href="{{ url('albums_delete', {'id': comment.id}) }}" title="{{ 'Delete album'|trans }}">{{ 'Delete album'|trans }}</a>#}
                    {#</td>#}
                </tr>
            {% endfor %}
            </tbody>
        </table>
    {#{% else %}#}
        {#<div class="alert alert-danger" role="alert">#}
            {#{{ 'Albums not found'|trans }}.#}
        {#</div>#}
    {#{% endif %}#}
{% endblock %}