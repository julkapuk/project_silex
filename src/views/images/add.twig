{# Add action template for Albums controller #}
{% extends 'base.twig' %}

{% block title %}{{  'Add image'|trans }}{% endblock %}

{% block content %}
    <h1>
        {{ 'Add image'|trans }}
    </h1>


  {% for flashMessage in app.session.getFlashBag.get('message') %}
    <div class="alert alert-{{ flashMessage.type }}">
        {{ flashMessage.content }}
    </div>
{% endfor %}

    </div>
    <form action="{{ url('image_add') }}" {{ form_enctype(form) }} method="post">
        {{ form_widget(form) }}
        <input type="submit" name="submit" value="{{ 'Save'|trans }}" />
    </form>
{% endblock %}


{#&#123;&#35; Add action template for Photos controller &#35;&#125;#}
